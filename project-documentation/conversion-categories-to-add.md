# Conversion Categories to Add (Based on Convert-me Sidebar)

This list compares the Convert-me sidebar categories to what exists in `resources/categories/*.json`. Categories already present in this package are omitted (with name mapping: weight->mass, computer storage->data_storage, data_transfer_rate->data_rate, fuel economy->fuel_consumption).

## Missing categories

### Basic
- Basic (`basic.html`)

### Historical
- Ancient and Historical (`historical.html`)
- Historical Weight (`history_weight`)
- Historical Length (`history_length`)
- Historical Volume (`history_volume`)
- Historical Area (`history_area`)

### Special
- Special (hub) (`special.html`)
- Cooking (`cooking`)
- Food Price (`food_price`)
- Weight to Volume (`weight2volume`)
- Fractions and Percent (`percent`)
- Numerals and Radix (`hex_octal_binary_roman`)
- Currency Rates (`currency`)
- Shoe Sizes (`shoesize`)
- Ring Sizes (`ringsize`)

### Engineering and science
- Engineering (hub) (`engineering.html`)
- Acceleration (`acceleration`)
- Density (`density`)
- Force (`force`)
- Circular measure (`circular`)
- Wire gauge (`wire_gauge`)
- Angular velocity (`angular_velocity`)
- Torque (`torque`)
- Temperature increment (`temperature-inc`)
- Flow rate by volume (`flow_rate_volume`)
- Flow rate by mass (`flow_rate_mass`)
- Illuminance (`illuminance`)
- Radiation (`radioactivity`)

### Computer
- Computer (hub) (`computer.html`)
